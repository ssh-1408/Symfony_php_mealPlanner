{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="mb-4">Edit User</h1>

    <form method="post">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Save</button>
        {{ form_end(form) }}
    </form>

    <a href="{{ path('admin_users_index') }}" class="btn btn-secondary mt-3">Back to List</a>
</div>
{% endblock %}
