{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center text-danger">Admin Panel</h1>

    <div class="row justify-content-center mt-4">
        <div class="col-md-4 text-center">
            <a href="{{ path('admin_users_index') }}" class="btn btn-outline-dark w-100 p-4 mb-3">
                <i class="fas fa-users-cog me-2"></i> Manage Users
            </a>
        </div>
        <div class="col-md-4 text-center">
            <a href="{{ path('admin_recipes_index') }}" class="btn btn-outline-dark w-100 p-4 mb-3">
                <i class="fas fa-utensils me-2"></i> Review Recipes
            </a>
        </div>
    </div>
</div>
{% endblock %}
