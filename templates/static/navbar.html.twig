<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm fixed-top">
    <div class="container-fluid">
        <img src="{{ asset('images/logo2.png')}}" alt="MealitUP Logo" width="120" height="100" class="me-2 rounded">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% set currentRoute = app.request.attributes.get('_route') %}

                <li class="nav-item">
                    <a class="nav-link nav-link-custom {% if currentRoute == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}">
                    Home</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-custom {% if currentRoute == 'app_about' %}active{% endif %}" href="{{ path('app_about') }}">How it works</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link nav-link-custom {% if currentRoute == 'app_recipe_index' %}active{% endif %}" href="{{ path('app_recipe_index') }}">Browse Recipe</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-custom {% if currentRoute == 'app_meal_plan_index' %}active{% endif %}" href="{{ path('app_meal_plan_index') }}">Meal Planner</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-custom {% if currentRoute == 'app_contact' %}active{% endif %}" href="{{ path('app_contact') }}">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-custom {% if currentRoute == 'app_team' %}active{% endif %}"  href="{{ path('app_team') }}">Team</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-custom {% if currentRoute == 'app_customers' %}active{% endif %}"  href="{{ path('app_customers') }}">Customers</a>
                </li>

                {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link text-warning fw-bold" href="{{ path('admin_dashboard') }}">🛠 Admin Dashboard</a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav flex-column flex-lg-row align-items-start align-items-lg-center">
                {% if app.user %}
                    <li class="nav-item mb-2 mx-2 mb-lg-0">
                        <span class="navbar-text">
                            👋 Welcome, <strong>{{ app.user.username }}</strong>
                        </span>
                    </li>
                    <li class="nav-item mb-2 mb-lg-0">
                        <a href="{{ path('app_logout') }}" class="btn custom-button">Log out</a>
                    </li>
                {% else %}
                    <li class="nav-item mb-2 mb-lg-0">
                        <a href="{{ path('app_register') }}" class="btn custom-button">Register</a>
                    </li>
                    <li class="nav-item mx-2">
                        <a href="{{ path('app_login') }}" class="btn custom-button">Log in</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
